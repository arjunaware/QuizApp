# QuizApp
A Spring Boot REST API for managing quizzes and MCQ questions. Includes CRUD operations, quiz creation with random questions, and result evaluation. Built using Spring Data JPA with MySQL and tested via Postman (no UI).
